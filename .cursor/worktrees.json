{
  "setup-worktree": [
    "bun install",
    "cp $ROOT_WORKTREE_PATH/apps/server/.env apps/server/.env || true",
    "cp $ROOT_WORKTREE_PATH/apps/web/.env apps/web/.env || true",
    "cp $ROOT_WORKTREE_PATH/packages/db/.env packages/db/.env || true"
  ]
}
