{
  "setup-worktree": [
    "bun install",
    "cp $ROOT_WORKTREE_PATH/.env .env",
    "cp $ROOT_WORKTREE_PATH/.env apps/server/.env",
    "cp $ROOT_WORKTREE_PATH/.env apps/web/.env",
    "cp $ROOT_WORKTREE_PATH/.env packages/db/.env"
  ]
}
